<div class="row">
  <div class="span6 offset3">
    <%= form_for(@post) do |f| %>
            <p>
              <strong>Title:</strong>
              <%= @post.title %>
            </p>

            <p>
              <strong>Text:</strong>
              <%= @post.text %>
            </p>

            <%= link_to 'Back', posts_path %>
    <% end %>
  </div>
</div>